const initialState = {}

export const contentsReducer = (state = initialState, action) => {
    switch (action.type) {
        case 'ACTION' : 
            return {}
            default : 
            return state
    }
}