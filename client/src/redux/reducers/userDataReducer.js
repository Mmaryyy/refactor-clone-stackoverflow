const initialState = {}

export const userDataReducer = (state = initialState, action) => {
    switch (action.type) {
        case 'ACTION' : 
            return {}
            default : 
            return state
    }
}