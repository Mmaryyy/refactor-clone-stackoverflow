const initialState = {}

export const answersReducer = (state = initialState, action) => {
    switch (action.type) {
        case 'ACTION' : 
            return {}
            default : 
            return state
    }
}