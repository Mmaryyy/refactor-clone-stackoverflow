const initialState = {
  answerList: [],
  currentAnswer: {
    content: {},
    author: {}
  }
}

export const answersReducer = (state = initialState, action) => {
    switch (action.type) {
            default : 
            return state
    }
}